# CI smoke trigger

This file intentionally creates a tiny pull request so the repository pull-request workflow can run and expose CI diagnostics.

If CI passes, this file can be deleted or the PR can be closed without merging.
