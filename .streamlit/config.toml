# deploy trigger: restore-sidebar-pages-v2
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Keep native sidebar page navigation enabled so st.navigation(position="sidebar")
# remains visible after branding/sidebar edits.
showSidebarNavigation = true
toolbarMode = "minimal"
