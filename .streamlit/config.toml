# deploy trigger: restore-stable-native-nav-v4
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Stable setting: keep Streamlit's native page list visible so navigation never disappears.
showSidebarNavigation = true
toolbarMode = "minimal"
