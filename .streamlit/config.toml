# deploy trigger: custom-brand-language-tools-v7
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Hide Streamlit's automatic file-list sidebar. Use the app's branded sidebar instead.
showSidebarNavigation = false
toolbarMode = "minimal"
